# 安全自检报告（对照 C 验收清单）

当前版本：原生微信小程序 · 纯本地 MVP（无登录、无后端、无网络请求）。以下逐条给出结论：通过 / 部分通过 / 不适用 / TODO。

## C 验收清单

1. **代码无硬编码密钥与敏感信息 —— 通过**  
   已全局检索：无 AppSecret、无接口密钥/Token/私钥；`project.config.json` 中的 appid 为开发者本人公开标识，不属于密钥。

2. **本地存储无敏感明文、会话有过期机制 —— 部分通过**  
   仅存本机：昵称、头像（base64）、行程/待办/习惯文本、训练记录；无手机号、身份证、地址等。无登录会话（不适用过期机制）。头像 base64 为本地明文，建议后续云同步时加密存储（TODO）。

3. **所有输入均有校验，无注入 / XSS 风险点 —— 部分通过**  
   已新增存储层统一校验：trim、类型、长度上限（行程名 60 / 地点 100 / 昵称与习惯 20 / 智能输入 300）、日期格式、时间格式、分类白名单、重复规则白名单、星期数组范围校验；非法输入回退默认值。WXML 自动转义，无富文本渲染，无字符串拼接执行代码。若接后端，服务端仍需二次校验（TODO）。

4. **请求全部 HTTPS + 域名白名单；关键接口有签名/防重放/频控 —— 不适用（当前无网络请求）**  
   当前零网络调用。后续接入云开发/服务端时：合法域名白名单、HTTPS、`timestamp+nonce+签名`、频控与参数化查询为必做项（TODO）。

5. **登录态与权限校验 —— 不适用（无账号体系）**  
   页面路由为固定白名单，分享入口为固定首页路径，无参数直达敏感页。相册仅在用户点选头像时申请，拒绝后有引导，不捆绑授权。

6. **隐私政策、授权弹窗、数据脱敏、注销入口 —— 部分通过**  
   “我的 → 隐私与协议”已内置说明；相册授权按需触发；提供数据导出与一键清空（本机无账号场景即注销等价）。正式发布前需在 mp 后台填写《用户隐私保护指引》并配置隐私接口（用户侧必做 TODO）。当前无手机号等敏感展示，脱敏不适用。

7. **UGC 内容安全检测 —— TODO（当前不上传）**  
   所有文本仅存本机，不发布、不分享他人。后续启用云同步/社区类功能前，必须接入 `msgSecCheck` / `mediaCheckAsync` 或服务端审核。

8. **发布配置压缩混淆、无调试后门 —— 通过**  
   `project.config.json` 已开启 `minified / es6 / postcss / ignoreUploadUnusedFiles`；全局检索无 `console.*`、无调试开关、无测试账号后门。

9. **日志脱敏与审计日志 —— 不适用（无日志上报）**  
   客户端不写日志、不上报。接入服务端后需建立脱敏日志与审计（操作人/时间/动作）（TODO）。

10. **风险点清单与建议**
    - 纯本地无备份：数据随设备存储，建议后续云同步（需加密存储 + 服务端校验）。
    - 头像以 base64 明文存本机：可接受，升级建议为加密或仅存缩略图。
    - “重要提醒”的到点推送需要微信订阅消息 + 云函数，属服务端能力（功能 TODO）。
    - 客户端时间/本地开关仅用于展示，业务判定须以服务端为准（后续接入时遵循）。
    - 发布前必须完成：后台隐私指引、类目与备案合规（备案已在进行）。

## 补充：内容安全与上架前确认（最新）

- 已接入基础本地敏感内容拦截（词表以码点存储）：新增/编辑行程标题与地点、习惯名称、昵称命中时弹窗提醒并阻止保存。
- 该拦截为基础版，无法覆盖全部违规词形；正式对外发布前若存在 UGC 展示/分享场景，必须通过云函数调用微信 msgSecCheck / mediaCheckAsync 做服务端内容安全校验。
- 数据隔离：当前为纯本地存储，无网络、无云开发、无账号体系；微信按「用户 + 小程序」隔离本地 Storage，A 用户数据不会出现在 B 用户手机上；代价是也不支持跨设备同步。
- 上架前仍需在 mp 后台完成：小程序备案、《用户隐私保护指引》与相册用途声明、服务类目与备案主体一致、审核材料与版本说明。